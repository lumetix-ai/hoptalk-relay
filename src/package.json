{
  "private": true,
  "scripts": {
    "build": "tailwindcss --input frontend/main.css --output build/static/app.css --minify",
    "watch": "tailwindcss --input frontend/main.css --output build/static/app.css --watch=always"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "tailwindcss": "4.3.3"
  }
}
